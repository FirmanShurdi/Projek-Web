<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Index Kartu Keluarga</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        .table th, .table td {
            vertical-align: middle;
        }
        .table-dark th {
            background-color: #343a40;
            color: white; /* Warna teks diubah menjadi putih agar terlihat jelas */
        }
    </style>
    <div class="container">
        <% if (messages && messages.success) { %>
            <div class="alert alert-info" role="alert">
                <%= messages.success %>
            </div>
        <% } %>
        
        <div class="row mb-3">
            <div class="col">
                <a href="/kartu_keluarga/create" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Tambah Kartu Keluarga
                </a>
            </div>
        </div>
        <div class="table-responsive">
            <table class="table table-bordered text-center align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>#</th>
                        <th>No KK</th>
                        <th>Alamat</th>
                        <th>RT</th>
                        <th>RW</th>
                        <th>Kode Pos</th>
                        <th>Desa Kelurahan</th>
                        <th>Kecamatan</th>
                        <th>Kabupaten Kota</th>
                        <th>Provinsi</th>
                        <th>Aksi</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (data && data.length > 0) { %>
                        <% data.forEach((row, index) => { %>
                            <tr>
                                <td><strong><%= index + 1 %></strong></td>
                                <td><%= row.no_kk %></td>
                                <td><%= row.alamat || '-' %></td>
                                <td><%= row.rt || '-' %></td>
                                <td><%= row.rw || '-' %></td>
                                <td><%= row.kode_pos || '-' %></td>
                                <td><%= row.desa_kelurahan || '-' %></td>
                                <td><%= row.kecamatan || '-' %></td>
                                <td><%= row.kabupaten_kota || '-' %></td>
                                <td><%= row.provinsi || '-' %></td>
                                <td>
                                    <a href="/kartu_keluarga/edit/<%= row.no_kk %>" class="btn btn-sm btn-warning">Edit</a>
                                    <a href="/kartu_keluarga/delete/<%= row.no_kk %>" class="btn btn-sm btn-danger">Hapus</a>
                                </td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="11" class="text-center">Tidak ada data</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>